import 'package:flutter/material.dart';
import 'placeholder_screen.dart';

// Stub files to avoid build errors while implementing
class HomeScreen extends StatelessWidget { const HomeScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Inicio'); }
class ProjectListScreen extends StatelessWidget { const ProjectListScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Proyectos'); }
class OfflineMapScreen extends StatelessWidget { const OfflineMapScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Mapa'); }
class SyncScreen extends StatelessWidget { const SyncScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Sincronizar'); }
class GeminiAssistantScreen extends StatelessWidget { const GeminiAssistantScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Asistente IA'); }
class SamplingWizardScreen extends StatelessWidget { const SamplingWizardScreen({super.key}); @override Widget build(BuildContext context) => const PlaceholderScreen(title: 'Muestreos'); }
